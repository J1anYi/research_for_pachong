---
wave: 1
depends_on: []
files_modified:
  - "MediaCrawler/viewer/static/js/app.js"
  - "MediaCrawler/viewer/static/js/api.js"
autonomous: true
requirements:
  - SORT-01
---

## Plan 01: 验证并修复卡片排序问题

### Goal
确保最新数据始终显示在列表顶部（时间降序）

### Context
- 后端 `notes.py` 第173行已有排序逻辑: `notes.sort(key=lambda x: x.get("time") or 0, reverse=True)`
- 前端 API 调用时没有传递排序参数，需要确认是否影响渲染顺序

### Tasks

#### Task 1: 验证后端排序是否正确工作
```xml
<read_first>
  - MediaCrawler/api/routers/notes.py (lines 172-175, 206-238)
  - MediaCrawler/viewer/static/js/api.js (lines 7-17)
</read_first>

<action>
验证 notes.py 中的排序逻辑:
1. 确认第173行存在: notes.sort(key=lambda x: x.get("time") or 0, reverse=True)
2. 确认 API 端点 list_notes() 使用 read_jsonl_files() 返回已排序的 notes

验证 api.js 中的参数传递:
1. 确认 getNotes() 支持 offset 和 limit 参数传递
</action>

<acceptance_criteria>
- grep "sort.*reverse.*True" notes.py 返回包含该行的结果
- grep "offset" api.js 返回包含 offset 参数处理的结果
</acceptance_criteria>
```

#### Task 2: 验证前端渲染顺序
```xml
<read_first>
  - MediaCrawler/viewer/static/js/app.js (lines 73-95, 97-118)
</read_first>

<action>
检查 loadNotes() 和 renderNotes() 函数:
1. loadNotes() 中 allNotes = data.notes 应直接使用后端返回数据
2. renderNotes() 中使用 forEach 遍历，顺序应与后端一致
3. 无额外的 sort 操作改变顺序
</action>

<acceptance_criteria>
- app.js 中不存在手动排序代码（如 notes.sort）
- renderNotes 函数直接渲染 data.notes 不做额外处理
</acceptance_criteria>
```

#### Task 3: 如有问题则修复
```xml
<action>
如果验证发现问题，执行以下修复:
1. 确保 API 调用正确传递分页参数
2. 确保 renderNotes 按接收顺序渲染
3. 如需前端二次排序，添加: allNotes.sort((a, b) => (b.time || 0) - (a.time || 0))
</action>

<acceptance_criteria>
- loadNotes 调用后 allNotes 按时间降序排列
- 页面刷新后最新数据显示在顶部
</acceptance_criteria>
```

### Must Haves
- [ ] 后端排序逻辑存在且正确
- [ ] 前端正确渲染排序后的数据
- [ ] 刷新页面后最新数据在前
