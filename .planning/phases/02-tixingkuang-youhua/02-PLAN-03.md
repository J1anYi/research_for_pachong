---
wave: 3
depends_on:
  - 02-PLAN-02
files_modified:
  - api/schemas/crawler.py
  - api/routers/websocket.py
  - api/routers/notes.py
autonomous: true
requirements:
  - BUG-03
  - NOTIF-01
---

# Plan 03: 后端发送新增数据和标题信息

## Problem

1. `DataUpdateMessage` 不包含 `new_count` 字段，前端只能显示固定数字
2. `DataUpdateMessage` 不包含 `titles` 字段，无法显示更新内容

## Solution

1. 扩展 `DataUpdateMessage` schema，添加 `new_count` 和 `titles` 字段
2. 在后端追踪每个平台的记录数量，计算增量
3. 读取最新记录，提取标题信息
4. 在 `broadcast_platform_update` 中包含这些信息

---

## Tasks

### Task 1: 扩展 DataUpdateMessage schema

<read_first>
- api/schemas/crawler.py
</read_first>

<action>
修改 `DataUpdateMessage` 类，添加 `new_count` 和 `titles` 字段。

**当前代码 (第 110-114 行):**
```python
class DataUpdateMessage(BaseModel):
    """WebSocket message for platform-specific data update"""
    type: str = "data_update"
    platform: str  # Platform identifier: "xhs", "dy", "bili", "zhihu", etc.
    timestamp: str = ""
```

**修改为:**
```python
class DataUpdateMessage(BaseModel):
    """WebSocket message for platform-specific data update"""
    type: str = "data_update"
    platform: str  # Platform identifier: "xhs", "dy", "bili", "zhihu", etc.
    timestamp: str = ""
    new_count: int = 0  # Number of new records added
    titles: list[str] = []  # Titles of new records (max 5)
```
</action>

<acceptance_criteria>
- `api/schemas/crawler.py` 中 `DataUpdateMessage` 包含 `new_count` 字段，默认值为 `0`
- `api/schemas/crawler.py` 中 `DataUpdateMessage` 包含 `titles` 字段，默认值为空列表
- `grep -n "new_count" api/schemas/crawler.py` 返回包含 `DataUpdateMessage` 类的行
- `grep -n "titles" api/schemas/crawler.py` 返回包含 `DataUpdateMessage` 类的行
</acceptance_criteria>

---

### Task 2: 添加平台记录计数追踪

<read_first>
- api/routers/websocket.py
</read_first>

<action>
在 `websocket.py` 中添加平台记录数追踪字典和相关函数。

**在 `manager = ConnectionManager()` 之后（约第 61 行后）添加:**
```python
# Track previous record counts per platform for delta calculation
_previous_counts: dict[str, int] = {}


def set_previous_count(platform: str, count: int) -> None:
    """Set the previous count for a platform."""
    _previous_counts[platform] = count


def get_previous_count(platform: str) -> int:
    """Get the previous count for a platform, default to 0."""
    return _previous_counts.get(platform, 0)


def calculate_new_count(platform: str, current_count: int) -> int:
    """Calculate new records count by comparing with previous count."""
    previous = get_previous_count(platform)
    new_count = max(0, current_count - previous)
    set_previous_count(platform, current_count)
    return new_count
```
</action>

<acceptance_criteria>
- `api/routers/websocket.py` 包含 `_previous_counts` 字典定义
- `api/routers/websocket.py` 包含 `calculate_new_count` 函数
- `grep -n "calculate_new_count" api/routers/websocket.py` 返回函数定义行
</acceptance_criteria>

---

### Task 3: 添加获取最新记录的 API 函数

<read_first>
- api/routers/notes.py
</read_first>

<action>
在 `notes.py` 中添加获取最新记录的函数。

**在文件末尾（最后一个路由之后）添加:**
```python
async def get_recent_notes(platform: str, limit: int = 5) -> list[dict]:
    """
    Get the most recent notes for a platform.
    Used by WebSocket broadcast to include titles in notifications.
    
    Args:
        platform: Platform identifier (xhs, dy, bili, zhihu)
        limit: Maximum number of records to return
        
    Returns:
        List of note dictionaries with title field
    """
    platform_dir = DATA_DIR / platform
    if not platform_dir.exists():
        return []
    
    notes = []
    
    # Try JSONL files first
    jsonl_dir = platform_dir / "jsonl"
    if jsonl_dir.exists():
        for jsonl_file in sorted(jsonl_dir.glob("*.jsonl"), key=lambda x: x.stat().st_mtime, reverse=True):
            try:
                async with aiofiles.open(jsonl_file, 'r', encoding='utf-8') as f:
                    lines = await f.readlines()
                    # Read from end (most recent first)
                    for line in reversed(lines):
                        line = line.strip()
                        if line:
                            try:
                                note = json.loads(line)
                                if note.get('title'):
                                    notes.append(note)
                                    if len(notes) >= limit:
                                        return notes
                            except json.JSONDecodeError:
                                continue
            except Exception as e:
                print(f"[Notes] Error reading {jsonl_file}: {e}")
                continue
    
    # Fallback to JSON directory
    if len(notes) < limit:
        json_dir = platform_dir / "json"
        if json_dir.exists():
            for json_file in sorted(json_dir.glob("*.json"), key=lambda x: x.stat().st_mtime, reverse=True):
                try:
                    async with aiofiles.open(json_file, 'r', encoding='utf-8') as f:
                        content = await f.read()
                        data = json.loads(content)
                        if isinstance(data, list):
                            for note in reversed(data):
                                if isinstance(note, dict) and note.get('title'):
                                    notes.append(note)
                                    if len(notes) >= limit:
                                        return notes
                except Exception as e:
                    print(f"[Notes] Error reading {json_file}: {e}")
                    continue
    
    return notes


async def get_platform_notes_count(platform: str) -> int:
    """
    Get the total count of notes for a platform.
    
    Args:
        platform: Platform identifier
        
    Returns:
        Total number of notes
    """
    platform_dir = DATA_DIR / platform
    if not platform_dir.exists():
        return 0
    
    count = 0
    
    # Count from JSONL files
    jsonl_dir = platform_dir / "jsonl"
    if jsonl_dir.exists():
        for jsonl_file in jsonl_dir.glob("*.jsonl"):
            try:
                async with aiofiles.open(jsonl_file, 'r', encoding='utf-8') as f:
                    lines = await f.readlines()
                    count += len([l for l in lines if l.strip()])
            except Exception:
                continue
    
    # Count from JSON files
    json_dir = platform_dir / "json"
    if json_dir.exists():
        for json_file in json_dir.glob("*.json"):
            try:
                async with aiofiles.open(json_file, 'r', encoding='utf-8') as f:
                    content = await f.read()
                    data = json.loads(content)
                    if isinstance(data, list):
                        count += len(data)
            except Exception:
                continue
    
    return count
```

**注意**: 需要确保文件顶部已有 `import aiofiles` 和 `import json` 导入。
</action>

<acceptance_criteria>
- `api/routers/notes.py` 包含 `import aiofiles` 导入语句
- `api/routers/notes.py` 包含 `get_recent_notes` 函数
- `api/routers/notes.py` 包含 `get_platform_notes_count` 函数
- `grep -n "import aiofiles" api/routers/notes.py` 返回导入行
- `grep -n "async def get_recent_notes" api/routers/notes.py` 返回函数定义行
- `grep -n "async def get_platform_notes_count" api/routers/notes.py` 返回函数定义行
</acceptance_criteria>

---

### Task 4: 修改 broadcast_platform_update 包含新记录信息

<read_first>
- api/routers/websocket.py
</read_first>

<action>
修改 `broadcast_platform_update` 函数，使其获取并包含新增记录的数量和标题。

**当前代码 (第 89-106 行):**
```python
async def broadcast_platform_update(platform: str):
    """
    Broadcast platform-specific data_update message to all connected WebSocket clients.
    This allows frontend to filter and route updates to specific viewer components.

    Args:
        platform: The platform identifier (e.g., "xhs", "dy", "bili", "zhihu")
    """
    try:
        message = DataUpdateMessage(
            type="data_update",
            platform=platform,
            timestamp=datetime.now().isoformat()
        )
        await manager.broadcast(message.model_dump())
        print(f"[WS] Broadcasted data_update for platform '{platform}'")
    except Exception as e:
        print(f"[WS] Error broadcasting data_update for platform '{platform}': {e}")
```

**修改为:**
```python
async def broadcast_platform_update(platform: str):
    """
    Broadcast platform-specific data_update message to all connected WebSocket clients.
    This allows frontend to filter and route updates to specific viewer components.

    Args:
        platform: The platform identifier (e.g., "xhs", "dy", "bili", "zhihu")
    """
    from .notes import get_recent_notes, get_platform_notes_count
    
    try:
        # Get current count and calculate new records
        current_count = await get_platform_notes_count(platform)
        new_count = calculate_new_count(platform, current_count)
        
        # Get recent notes for titles
        recent_notes = await get_recent_notes(platform, limit=5)
        titles = [note.get('title', '') for note in recent_notes if note.get('title')]
        
        message = DataUpdateMessage(
            type="data_update",
            platform=platform,
            timestamp=datetime.now().isoformat(),
            new_count=new_count,
            titles=titles
        )
        await manager.broadcast(message.model_dump())
        print(f"[WS] Broadcasted data_update for platform '{platform}': {new_count} new, titles: {titles[:2]}")
    except Exception as e:
        print(f"[WS] Error broadcasting data_update for platform '{platform}': {e}")
```
</action>

<acceptance_criteria>
- `broadcast_platform_update` 函数调用 `get_recent_notes` 和 `get_platform_notes_count`
- `DataUpdateMessage` 构造包含 `new_count` 和 `titles` 参数
- `grep -A20 "async def broadcast_platform_update" api/routers/websocket.py` 输出包含 `new_count` 和 `titles`
</acceptance_criteria>

---

## Verification

1. 启动服务器，运行爬虫
2. 观察控制台输出，应看到类似:
   ```
   [WS] Broadcasted data_update for platform 'xhs': 3 new, titles: ['标题1', '标题2']
   ```
3. 前端通知应显示实际新增数量和标题

---

## Success Criteria

- [ ] WebSocket 消息包含 `new_count` 字段，显示实际新增数量
- [ ] WebSocket 消息包含 `titles` 字段，显示新记录标题
- [ ] 前端通知正确显示增量数量（如 "新增 3 条数据"）
- [ ] 前端通知显示具体的标题内容
