---
wave: 2
depends_on:
  - 02-PLAN-01
files_modified:
  - viewer/static/js/notifications.js
autonomous: true
requirements:
  - NOTIF-03
---

# Plan 02: 添加通知去重机制

## Problem

短时间内可能收到多个相同或相似的通知，导致重复弹窗。需要实现基于内容的去重机制。

## Solution

在 `notifications.js` 中添加去重 Map，使用 `platform + titles + count` 作为哈希键，在 5 秒时间窗口内跳过重复通知。

---

## Tasks

### Task 1: 添加去重数据结构和配置

<read_first>
- viewer/static/js/notifications.js
</read_first>

<action>
在 `notifications.js` 的 IIFE 内部，`PLATFORM_CONFIG` 定义之后，添加去重相关的数据结构和配置。

**在第 19 行后添加:**
```javascript
// 通知去重 - 基于 content hash 和时间窗口
const NOTIFICATION_DEDUP = {
    recentNotifications: new Map(),  // hash -> timestamp
    dedupWindowMs: 5000,             // 5 秒去重窗口
    cleanupIntervalMs: 30000         // 30 秒清理一次
};
```
</action>

<acceptance_criteria>
- `viewer/static/js/notifications.js` 包含 `NOTIFICATION_DEDUP` 常量定义
- `grep -n "NOTIFICATION_DEDUP" viewer/static/js/notifications.js` 输出包含 `recentNotifications`、`dedupWindowMs`、`cleanupIntervalMs`
</acceptance_criteria>

---

### Task 2: 创建去重哈希函数

<read_first>
- viewer/static/js/notifications.js
</read_first>

<action>
在 `showDataNotification` 函数之前，添加去重哈希生成函数和清理函数。

**在 `init` 函数之后，`showDataNotification` 函数之前（约第 40 行后）添加:**
```javascript
/**
 * 生成通知去重哈希
 * @param {Object} data - 通知数据
 * @returns {string} 哈希字符串
 */
function generateNotificationHash(data) {
    // 使用 platform + titles + count 作为去重键
    const titles = data.titles ? data.titles.slice(0, 3).join(',') : '';
    const count = data.count || 0;
    return `${data.platform}-${titles}-${count}`;
}

/**
 * 检查是否为重复通知
 * @param {string} hash - 通知哈希
 * @returns {boolean} true 表示是重复通知，应跳过
 */
function isDuplicateNotification(hash) {
    const now = Date.now();
    const lastShown = NOTIFICATION_DEDUP.recentNotifications.get(hash);
    
    if (lastShown && (now - lastShown < NOTIFICATION_DEDUP.dedupWindowMs)) {
        return true;
    }
    
    // 记录本次通知时间
    NOTIFICATION_DEDUP.recentNotifications.set(hash, now);
    
    // 定期清理过期条目（每 30 秒）
    if (NOTIFICATION_DEDUP.recentNotifications.size > 50) {
        for (const [key, timestamp] of NOTIFICATION_DEDUP.recentNotifications) {
            if (now - timestamp > NOTIFICATION_DEDUP.cleanupIntervalMs) {
                NOTIFICATION_DEDUP.recentNotifications.delete(key);
            }
        }
    }
    
    return false;
}
```
</action>

<acceptance_criteria>
- `viewer/static/js/notifications.js` 包含 `generateNotificationHash` 函数
- `viewer/static/js/notifications.js` 包含 `isDuplicateNotification` 函数
- `grep -n "function generateNotificationHash" viewer/static/js/notifications.js` 返回单行匹配
- `grep -n "function isDuplicateNotification" viewer/static/js/notifications.js` 返回单行匹配
</acceptance_criteria>

---

### Task 3: 在 showDataNotification 中应用去重逻辑

<read_first>
- viewer/static/js/notifications.js
</read_first>

<action>
修改 `showDataNotification` 函数，在函数开头添加去重检查。

**当前函数开头 (约第 49-51 行):**
```javascript
function showDataNotification(data) {
    if (!notificationContainer) init();
```

**修改为:**
```javascript
function showDataNotification(data) {
    if (!notificationContainer) init();

    // 去重检查
    const hash = generateNotificationHash(data);
    if (isDuplicateNotification(hash)) {
        console.log('[通知] 去重，跳过:', hash);
        return;
    }
```
</action>

<acceptance_criteria>
- `showDataNotification` 函数开头包含 `generateNotificationHash` 调用
- `showDataNotification` 函数开头包含 `isDuplicateNotification` 调用
- 去重时会执行 `return` 跳过通知显示
- `grep -A5 "function showDataNotification" viewer/static/js/notifications.js` 输出包含 `isDuplicateNotification`
</acceptance_criteria>

---

### Task 4: 支持传递 titles 参数到通知

<read_first>
- viewer/static/js/notifications.js
</read_first>

<action>
修改通知内容构建部分，支持显示 titles 列表。

**当前 detail 构建逻辑 (约第 58-60 行):**
```javascript
const detail = data.keyword
    ? `关键词: ${data.keyword}`
    : (data.count ? `新增 ${data.count} 条数据` : '数据已更新');
```

**修改为:**
```javascript
let detail;
if (data.titles && data.titles.length > 0) {
    // 显示标题列表
    const displayTitles = data.titles.slice(0, 2);
    const remaining = data.titles.length - displayTitles.length;
    detail = displayTitles.map(t => `"${t}"`).join(', ');
    if (remaining > 0) {
        detail += ` 等${data.titles.length}条`;
    }
} else if (data.keyword) {
    detail = `关键词: ${data.keyword}`;
} else if (data.count) {
    detail = `新增 ${data.count} 条数据`;
} else {
    detail = '数据已更新';
}
```
</action>

<acceptance_criteria>
- `showDataNotification` 函数支持 `data.titles` 参数
- 当 `data.titles` 存在时，显示标题内容
- `grep -n "data.titles" viewer/static/js/notifications.js` 至少返回 2 行匹配
</acceptance_criteria>

---

## Verification

1. 在浏览器控制台测试去重逻辑:
   ```javascript
   // 第一次调用 - 应显示
   DataNotification.show({platform: 'xhs', count: 5, titles: ['标题A']});
   // 第二次相同内容 - 应跳过（console 显示 "去重，跳过"）
   DataNotification.show({platform: 'xhs', count: 5, titles: ['标题A']});
   // 不同内容 - 应显示
   DataNotification.show({platform: 'xhs', count: 5, titles: ['标题B']});
   ```
2. 等待 5 秒后再次发送相同通知，应该能正常显示

---

## Success Criteria

- [ ] 相同内容通知在 5 秒内只显示一次
- [ ] 不同内容通知正常显示
- [ ] 5 秒后可以再次显示相同内容通知
- [ ] 旧条目被定期清理，不会内存泄漏
