---
wave: 2
depends_on: ["09-PLAN-01", "09-PLAN-02"]
files_modified:
  - MediaCrawler/viewer/static/js/modal.js
  - MediaCrawler/viewer/static/css/style.css
requirements_addressed:
  - CAROUSEL-02
  - CAROUSEL-03
  - CAROUSEL-04
  - CAROUSEL-05
  - CAROUSEL-06
autonomous: true
---

# Plan 03: 模态框轮播组件

<objective>
实现图片轮播交互：圆点指示器、自动轮播、鼠标悬停暂停、点击重置计时器。

</objective>

<background>
当前模态框已有图片导航功能（左右箭头、键盘支持），需要添加：
1. 圆点指示器
2. 自动轮播（3秒间隔）
3. 鼠标悬停暂停
4. 点击重置计时器

</background>

<read_first>
- MediaCrawler/viewer/static/js/modal.js — 完整文件
- .planning/phases/09-xiaohongshu-duotu-lunbo-xianshi/09-CONTEXT.md — D-02 到 D-08 决策

</read_first>

<tasks>

## Task 1: 添加圆点指示器

<action>
修改 MediaCrawler/viewer/static/js/modal.js：

1. 在 openNoteModal() 函数中，创建圆点指示器容器
2. 添加 createDotIndicators() 函数
3. 修改 updateGallery() 函数，更新圆点状态
4. 在 HTML 中添加圆点容器
5. 添加 CSS 样式

</action>

<acceptance_criteria>
- modal.js 包含 createDotIndicators() 函数
- 圆点数量等于图片数量
- 当前图片对应的圆点有 active 类
- 点击圆点切换到对应图片
- CSS 包含 .gallery-dots 和 .gallery-dot 样式

</acceptance_criteria>

## Task 2: 实现自动轮播

<action>
修改 MediaCrawler/viewer/static/js/modal.js：

1. 添加自动轮播状态变量 autoPlayTimer
2. 添加 startAutoPlay() 函数
3. 添加 stopAutoPlay() 函数
4. 添加 resetAutoPlay() 函数
5. 在 openNoteModal() 中启动自动轮播
6. 在 closeNoteModal() 中停止自动轮播

</action>

<acceptance_criteria>
- modal.js 包含 autoPlayTimer 变量
- modal.js 包含 startAutoPlay()、stopAutoPlay()、resetAutoPlay() 函数
- 打开模态框时自动开始轮播
- 关闭模态框时停止轮播
- 轮播间隔为 3000ms

</acceptance_criteria>

## Task 3: 鼠标悬停暂停和点击重置

<action>
修改 MediaCrawler/viewer/static/js/modal.js：

1. 在 setupModalEvents() 中添加鼠标事件
2. 鼠标进入图片区域时停止轮播
3. 鼠标离开图片区域时恢复轮播
4. 点击图片区域时重置轮播计时器

</action>

<acceptance_criteria>
- 鼠标进入图片区域时停止轮播
- 鼠标离开图片区域时恢复轮播
- 点击图片区域时重置轮播计时器

</acceptance_criteria>

## Task 4: 修改 openNoteModal 接收完整图片列表

<action>
修改 MediaCrawler/viewer/static/js/modal.js：

1. 修改 openNoteModal() 使用 note.image_urls 作为主要图片列表
2. 修改 updateGallery() 支持图片 URL 列表

</action>

<acceptance_criteria>
- openNoteModal() 使用 note.image_urls 作为主要图片列表
- openNoteModal() 使用 note.remote_image_urls 作为后备
- 索引显示正确（如 1/5）
- 圆点指示器数量与图片数量一致

</acceptance_criteria>

</tasks>

<verification>
**Functional tests:**
1. 打开一篇多图笔记
2. 确认圆点指示器显示正确
3. 确认自动轮播每3秒切换
4. 鼠标悬停在图片上，确认轮播暂停
5. 鼠标离开，确认轮播恢复
6. 点击图片，确认计时器重置
7. 点击圆点，确认切换到对应图片

</verification>

---
*Plan: 09-03-modal-carousel*
*Created: 2026-04-24*
