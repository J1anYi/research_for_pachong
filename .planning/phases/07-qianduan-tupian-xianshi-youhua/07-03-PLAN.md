---
phase: 07-qianduan-tupian-xianshi-youhua
plan: 03
type: execute
wave: 3
depends_on:
  - 07-02
files_modified:
  - MediaCrawler/viewer/static/css/style.css
  - MediaCrawler/viewer/static/js/app.js
autonomous: true
requirements:
  - FE-04
user_setup: []
---

<objective>
添加图片加载状态显示，提升用户体验。

Purpose: 用户能看到图片加载状态（加载中、加载成功、加载失败）。
Output: Modified MediaCrawler/viewer/static/css/style.css, MediaCrawler/viewer/static/js/app.js
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-qianduan-tupian-xianshi-youhua/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add image loading state styles</name>
  <files>MediaCrawler/viewer/static/css/style.css</files>
  <read_first>
    - MediaCrawler/viewer/static/css/style.css (current implementation)
  </read_first>
  <action>
Add to `MediaCrawler/viewer/static/css/style.css`:

```css
/* Image loading states */
.note-card-image {
    transition: opacity 0.3s ease;
}

.note-card-image.image-loading {
    opacity: 0;
    background: linear-gradient(90deg, #f0f0f0 25%, #e0e0e0 50%, #f0f0f0 75%);
    background-size: 200% 100%;
    animation: shimmer 1.5s infinite;
}

.note-card-image.image-loaded {
    opacity: 1;
}

.note-card-image.image-error {
    opacity: 0;
}

@keyframes shimmer {
    0% { background-position: 200% 0; }
    100% { background-position: -200% 0; }
}

/* Loading spinner overlay */
.image-loading-overlay {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.8);
}

.image-loading-overlay::after {
    content: '';
    width: 30px;
    height: 30px;
    border: 3px solid #e0e0e0;
    border-top-color: #666;
    border-radius: 50%;
    animation: spin 0.8s linear infinite;
}

@keyframes spin {
    to { transform: rotate(360deg); }
}
```
  </action>
  <verify>
    <automated>grep -n "image-loading\|image-loaded\|shimmer" MediaCrawler/viewer/static/css/style.css</automated>
  </verify>
  <done>
    - CSS classes for loading states added
    - Shimmer animation for loading state
    - Spinner animation added
  </done>
</task>

<task type="auto">
  <name>Task 2: Add loading state handling to createNoteCard</name>
  <files>MediaCrawler/viewer/static/js/app.js</files>
  <read_first>
    - MediaCrawler/viewer/static/js/app.js (current implementation)
  </read_first>
  <action>
Modify `createNoteCard()` function in `MediaCrawler/viewer/static/js/app.js`:

1. **Add loading state class on image creation:**
   ```javascript
   const img = document.createElement('img');
   img.className = 'note-card-image lazy image-loading';
   ```

2. **Add onload handler:**
   ```javascript
   img.onload = function() {
       this.classList.remove('image-loading');
       this.classList.add('image-loaded');
   };
   ```

3. **Update onerror handler:**
   ```javascript
   img.onerror = function() {
       if (this.dataset.remoteUrl && this.dataset.fallbackUsed === 'false') {
           console.log('[XHS] Local image failed, falling back to remote:', this.dataset.remoteUrl);
           this.dataset.fallbackUsed = 'true';
           this.classList.remove('image-loaded');
           this.classList.add('image-loading');
           this.src = this.dataset.remoteUrl;
       } else {
           this.classList.remove('image-loading');
           this.classList.add('image-error');
           this.style.display = 'none';
           const placeholder = document.createElement('div');
           placeholder.className = 'note-card-placeholder';
           placeholder.textContent = '🖼️';
           imageContainer.appendChild(placeholder);
       }
   };
   ```

4. **Also update lazy loading handler to manage states:**
   - In `setupLazyLoading()` function, ensure loading state is set when image starts loading
  </action>
  <verify>
    <automated>grep -n "image-loading\|image-loaded\|img.onload" MediaCrawler/viewer/static/js/app.js</automated>
  </verify>
  <done>
    - Images show loading state initially
    - Images show loaded state on success
    - Images show error state on failure
    - Fallback preserves loading state
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None - CSS and JS changes only affect presentation.

## STRIDE Threat Register

No security threats identified for CSS/JS presentation changes.
</threat_model>

<verification>
1. Images show shimmer animation while loading
2. Images transition smoothly when loaded
3. Error state shows placeholder
4. Loading states work with fallback mechanism
</verification>

<success_criteria>
- 图片加载中显示骨架屏/动画
- 图片加载成功后平滑过渡
- 图片加载失败显示错误占位符
- 与 fallback 机制协同工作
</success_criteria>

<output>
After completion, create `.planning/phases/07-qianduan-tupian-xianshi-youhua/07-03-SUMMARY.md`
</output>
